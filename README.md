# 지성인 개발자 포트폴리오

React·Next.js 실무 경험을 바탕으로 풀스택으로 확장하는 프론트엔드 개발자
지성인의 한국어 포트폴리오입니다.

## 실행

```bash
npm install
npm run dev
npm run build
```

콘텐츠는 `src/content.js`에서 관리합니다. TECHZONE 공개 배포가 완료되면 프로젝트
설정의 `liveUrl`을 입력하고 `published`를 `true`로 바꾸면 메인 카드와 상세
Case Study가 함께 활성화됩니다.
